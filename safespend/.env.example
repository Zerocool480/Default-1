# PostgreSQL connection
DATABASE_URL=postgresql://safespend:safespend_dev@localhost:5432/safespend

# Long random string for signing session tokens
JWT_SECRET=change-me-to-a-long-random-string

# API server port (Vite dev server proxies /api here)
PORT=5001

# --- Not needed until M6 (bank connectivity) ---
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ENV=sandbox
# PLAID_TOKEN_KEY=   # 32-byte hex key for AES-256-GCM encryption of access tokens

# --- Not needed until M11 (copilot) / M12 (AI categorization) ---
# AI_ENABLED=false
# ANTHROPIC_API_KEY=
